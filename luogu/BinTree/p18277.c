#include<stdio.h>

#define MAX 26

int main(void)
{
	char DLR[MAX],LDR[MAX];
	int rootidx = 0,i = 0;
	scanf("%26s",DLR);
	scanf("%26s",LDR);
	while(1)
	{
		for(i = 0;i<26;i++){
			if(LDR[i] == ROOT[])
		} 
	}
	return 0;
}
